# AGENTS.md

이 파일은 AI 에이전트가 이 저장소에서 작업할 때 참조하는 도메인 지식 및 요구사항 명세다.
기술적인 구현 내용은 [docs/PLAN.md](docs/PLAN.md) 를 참조한다.

---

## 시스템 개요

가상의 반도체 회사 "S-Semi"의 **시료(Sample) 생산·주문·재고·출고 관리**를 위한 콘솔 기반 시스템.
담당자가 명령을 직접 입력하며, 주문 상태 흐름에 따라 모든 처리가 제어된다.

고객(시료 요청자)이 필요한 시료를 이메일로 요청하면, 주문 담당자가 주문서를 작성하여 생산 담당자에게 전달한다.
생산 담당자는 시료를 등록하고, 주문 수신 후 승인 또는 거절한다.

생산 라인은 공장에서 시료를 하나씩 생산하는 설비 흐름이며, 
현재 생산 라인은 단 한개이다.
하나의 생산 라인은 시료를 하나씩 생산하며, 생산 라인은 시료를 한번에 하나씩밖에 생산하지 못한다.
주문이 들어온 시료에 대해서만 생산한다.

---

## 주문 상태 흐름 (Order Status)

```
RESERVED  → CONFIRMED  (주문 승인 + 재고 충분 → 출고 대기)
RESERVED  → PRODUCING  (주문 승인 + 재고 부족 → 생산 중)
RESERVED  → REJECTED   (주문 거절)
PRODUCING → CONFIRMED  (생산 완료)
CONFIRMED → RELEASE    (출고 처리, 재고 차감)
```

> 모니터링에서 `REJECTED` 상태는 제외한다.

---

## 기능 명세

### ① 메인 메뉴
- 메뉴 항목: 시료 관리 / 주문(접수·승인·거절) / 모니터링 / 출고 처리 / 생산 라인
- 전체 시료 요약 정보를 화면에 표시한다

### ② 시료 관리

| 기능 | 설명 |
|---|---|
| 시료 등록 | 시료 ID, 이름, 평균 생산시간, 수율을 입력하여 등록 |
| 시료 조회 | 등록된 전체 시료 목록 + 현재 재고 수량 함께 표시 |
| 시료 검색 | 이름 등 속성값으로 특정 시료 검색 |

시스템에 등록된 시료만 주문 가능하다.

### ③ 시료 주문 (접수)
고객이 시료를 요청하면 주문 담당자가 주문을 생성한다.
- 입력: 시료 ID, 고객명, 주문 수량
- 생성 시 주문 상태는 `RESERVED`

### ④ 주문 승인/거절
접수된 주문(`RESERVED`) 목록을 확인하고 특정 주문에 대해 승인 또는 거절한다.

| 기능 | 처리 |
|---|---|
| 접수 목록 조회 | `RESERVED` 상태 주문만 표시 |
| 주문 승인 — 재고 충분 | 즉시 `CONFIRMED` 전환 |
| 주문 승인 — 재고 부족 | 생산 라인 자동 등록 + `PRODUCING` 전환 |
| 주문 거절 | 즉시 `REJECTED` 전환 |

### ⑤ 모니터링
담당자가 현재 시스템의 상태를 한눈에 파악할 수 있도록 정보를 제공한다.

- **주문량 확인**: `RESERVED` / `CONFIRMED` / `PRODUCING` / `RELEASE` 상태별 주문 목록·수량 표시 (`REJECTED` 제외)
- **재고량 확인**: 시료별 재고 수량 + 아래 재고 상태 함께 표기

| 재고 상태 | 조건 |
|---|---|
| 여유 | 주문 대비 재고가 충분 |
| 부족 | 주문 대비 재고 수량이 부족 |
| 고갈 | 현재 재고 = 0 |

### ⑥ 생산 라인
재고 부족분에 대해서만 생산을 진행하며, 수율 및 오차를 고려하여 스케줄링한다.
스케줄링 전략은 **FIFO (First-In, First-Out)** 큐를 사용한다.

| 기능 | 설명 |
|---|---|
| 생산 현황 표기 | 현재 생산 중인 시료 정보(주문 정보, 현재까지의 생산량 등) 표기 |
| 대기 주문 확인 | 생산 큐에서 대기 중인 목록 출력 |
| 생산 완료 처리 | `PRODUCING` → `CONFIRMED` 상태 전환 |

### ⑦ 출고 처리
- 대상: `CONFIRMED` 상태 주문
- 출고 실행 시 주문 상태 `RELEASE` 전환 + 해당 시료 재고 차감

---

## 비즈니스 규칙

1. 등록되지 않은 시료는 주문 불가
2. 주문 승인 시 재고 비교 후 `CONFIRMED` 또는 `PRODUCING` 자동 분기
3. 생산 라인은 부족분만 처리하며 수율·오차(×0.9)를 고려해 올림 계산
4. 출고 시점에 재고 차감 (승인 시점 아님)
5. 모니터링에서 `REJECTED` 주문은 집계·표시 제외
